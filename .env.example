# Example only. Do not put real secrets in a GitHub commit.
# Enter these in the private environment/Secrets section of your Python host.
FLASK_SECRET_KEY=REPLACE_WITH_A_LONG_RANDOM_SECRET
COOKIE_SECURE=1
INITIAL_ADMIN_EMAIL=ziyaullahshahid@gmail.com
# INITIAL_ADMIN_PASSWORD: set privately for ONE first deployment, then REMOVE it.
# ZIYA_DB_PATH: set to the host's persistent disk path if not using ./data/site.db.
